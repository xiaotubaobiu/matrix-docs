---
title: "Claude 系列模型"
description: "Anthropic Claude Opus 系列模型的使用说明"
---

Matrix 已上架 Anthropic Claude 的 **Opus 旗舰系列**。Opus 是 Claude 综合能力最强的型号,擅长长上下文、深度推理和复杂代码。

## 已上架模型

| 模型 | 定位 |
| --- | --- |
| `claude-opus-4-8` | **最新旗舰** |
| `claude-opus-4-7` | Opus 系列 |
| `claude-opus-4-6` | Opus 系列 |

> 日常使用建议选最新的 `claude-opus-4-8`。三个版本都是 Opus 旗舰级,数字越大越新。

## Opus 系列定位

Opus 是 Claude 的**顶级系列**,综合能力最强,尤其擅长:

- **超长上下文**:支持数十万 token 的上下文窗口,适合长文档分析、大型代码库
- **代码能力强**:在代码生成、调试上表现优异
- **写作质量高**:文笔自然,适合内容创作、文档撰写
- **指令遵循好**:严格按要求输出,适合结构化任务

作为旗舰系列,Opus 质量最高,适合对效果要求高的场景。

## 版本怎么选?

三个上架的版本都是 Opus 旗舰,能力接近:

| 版本 | 建议 |
| --- | --- |
| `claude-opus-4-8` | **首选**,最新版本,能力与稳定性最佳 |
| `claude-opus-4-7` | 次新,可作备选 |
| `claude-opus-4-6` | 较早版本 |

如果是新项目或重要任务,直接用 `claude-opus-4-8`。如果某些场景对特定旧版本有兼容需求,再用 4-7 / 4-6。

## 调用示例

### 基础对话(OpenAI 兼容格式)

```bash
curl https://matrix.000328.xyz/v1/chat/completions \
  -H "Authorization: Bearer sk-你的Token" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "claude-opus-4-8",
    "messages": [{"role": "user", "content": "帮我写一封礼貌的请假邮件"}],
    "max_tokens": 1024,
    "stream": false
  }'
```

### Anthropic 原生格式(Messages API)

```bash
curl https://matrix.000328.xyz/v1/messages \
  -H "Authorization: Bearer sk-你的Token" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "claude-opus-4-8",
    "max_tokens": 1024,
    "messages": [{"role": "user", "content": "解释什么是闭包"}]
  }'
```

<Note>
  Matrix 同时支持 **OpenAI 兼容格式**(`/v1/chat/completions`)和 **Anthropic 原生格式**(`/v1/messages`)。Claude Code 等原生客户端会自动用后者。
</Note>

### 用 Anthropic SDK 调用

```python
import anthropic

client = anthropic.Anthropic(
    api_key="sk-你的Token",
    base_url="https://matrix.000328.xyz"
)

message = client.messages.create(
    model="claude-opus-4-8",
    max_tokens=1024,
    messages=[{"role": "user", "content": "你好"}]
)
print(message.content[0].text)
```

## 给 Claude Code 用户

Claude Code 原生使用 Anthropic 协议,配置时需要:

- `ANTHROPIC_BASE_URL`: `https://matrix.000328.xyz`
- `ANTHROPIC_AUTH_TOKEN`: 你的 Token

详细配置见 [Claude Code 配置](/cli-config/claude-code)。

## 选型建议

| 场景 | 推荐 |
| --- | --- |
| 任何 Opus 场景(首选) | `claude-opus-4-8` |
| 长文档分析 | `claude-opus-4-8` |
| 编程写代码 | `claude-opus-4-8` |

> 如果以上模型不能满足需求,可联系管理员反馈,我们会评估上架更多型号。
